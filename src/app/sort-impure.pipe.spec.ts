import { SortImpurePipe } from './sort-impure.pipe';

describe('SortImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new SortImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
